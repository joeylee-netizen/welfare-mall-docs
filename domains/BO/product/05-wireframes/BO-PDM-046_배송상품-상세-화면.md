---
id: "BO-PDM-046"
title: "배송상품 상세 화면"
type: wireframe
domain: product
status: draft
version: "1.4"
created: 2026-04-16
updated: 2026-04-16
author: "기획자"
refs:
  - "BO-PDM-024"
  - "BO-PDM-040"
tags: [상품, 배송, 상세, 화면, 편집, 탭]
route: "/bo/products/delivery/:id"
layout: bo-standard
mockup_type: html
mockup_path: "./BO-PDM-046_배송상품-상세-화면.html"
---

# 배송상품 상세 화면

## 수정 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| v1.4 | 2026-04-16 | 기획자 | 탭 3~7 전면 재설계: 판매정보(5영역), 옵션/재고(편집폼+옵션등록팝업), 배송정보(3영역: 출고지/반품/교환), 상세정보(3영역: 이미지/동영상/상세설명), 상품정보제공고시(편집테이블). 디스크립션 패널 전체 갱신. JS 함수 7개 추가. |
| v1.3 | 2026-04-16 | 기획자 | 전시정보 탭 편집 가능 폼 전면 재설계 (6필드: 전시여부/노출채널/전시기간/담당MD/표준카테고리/전시카테고리), 담당MD 팝업 추가, 이미지 영역 제거 |
| v1.2 | 2026-04-16 | 기획자 | 상단 요약 헤더 제거(중복), 영역별 카드 분리형 UI 적용, 등록정보 읽기전용 배경 차별화 |
| v1.1 | 2026-04-16 | 기획자 | 기본정보 탭 편집 가능 폼 전면 재설계 (5개 영역 분할, 팝업 3개, 하단 버튼 변경) |
| v1.0 | 2026-04-16 | 기획자 | 초안 작성 |

## 화면 개요

배송상품 목록([BO-PDM-040](./BO-PDM-040_배송상품-목록-화면.md))에서 상품명 클릭 시 진입하는 **상품 상세 화면**이다. 7개 탭 전체가 편집 가능한 폼으로 제공된다. 하단에 승인 이력과 저장/취소 버튼을 제공한다.

## 진입 조건

- 배송상품 목록(BO-PDM-040)에서 **상품명 링크 클릭**
- 대상: 승인완료(APPROVED) 상태 상품
- 흐름: 목록(BO-PDM-040) → **상세(BO-PDM-046)** → 저장

## 레이아웃

- **GNB**: 다크 헤더(#1a1a2e) — 로고, BO 관리자, 사용자 정보
- **LNB**: 확장형 사이드바 — 상품 관리 그룹 확장, "배송상품 목록" 활성
- **Content**: 7개 탭 → 탭 콘텐츠(카드 분리형) → 승인 이력 → 하단 버튼
- **영역 구분**: 각 `.form-section`이 카드(border + shadow + padding) 형태로 시각적 분리. 콘텐츠 영역 배경(`#f4f6f8`)과 카드 배경(`#fff`) 대비로 가독성 확보.
- **등록정보 차별화**: `.form-section--readonly` 클래스 — 읽기전용 영역은 `#f9fafb` 배경으로 편집 영역과 구분.

## 구성 요소

### 7개 탭 구성

#### 탭 1: 기본정보 (편집 가능 폼 — 5개 영역)

##### 영역 1: 기본정보

2열 form-grid 레이아웃.

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 상품명 | `<input>` (full width) | 필수(*) |
| 상품코드 | readonly-field | 자동 생성 13자리 |
| 판매사 상품코드 | `<input>` | 선택 |
| 다국어 상품명 | `<input>` (full width) | 선택 |
| 판매사 | `<input readonly>` + [조회] 버튼 | 팝업 선택, 우측 하위업체 `<select>` 연동 |
| 하위업체 | `<select>` | 판매사 선택 시 활성화 |
| 브랜드 | `<input readonly>` + [조회] 버튼 | 팝업 선택 |
| 모델명 | `<input>` | |
| 상품키워드 | `<input>` + [등록하기] 버튼 | 하단 태그(chip) 형태 출력, x 삭제 |

##### 영역 2: 추가정보

2열 form-grid 레이아웃.

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 제조자 | `<input>` | |
| 원산지 | `<input>` | |
| 상품리뷰 등록 | radio: 가능(기본) / 불가능 | |
| Q&A 등록 | radio: 가능(기본) / 불가능 | |
| 선물하기 | radio: 가능(기본) / 불가능 | |
| 최소 구매수량 | radio(사용안함/사용) + number input | "개 이상부터 구매 가능", 도움말: "1회당 구매 가능한 최소 수량" |
| 최대 구매수량 | radio(사용안함/사용) + number input | "개 이하까지 구매 가능", 도움말: "1회당 구매 가능한 최대 수량" |
| 특가 구매수량 | radio(사용안함/사용) + number input | "개 까지 구매 가능", 도움말: "판매기간 동안 구매 가능한 수량" |

- 구매수량 3종: "사용안함" 선택 시 숫자 입력 disabled, "사용" 선택 시 활성화

##### 영역 3: 고객사 정보

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 노출 제외 고객사 | radio: 미등록(기본) / 등록 | |
| 고객사 입력 | `<input readonly>` + [조회] + [등록] 버튼 | "등록" 선택 시 활성화 |
| 등록 목록 | 태그(chip) 출력 | x 클릭으로 삭제 |

- "미등록" 선택 시 하위 입력 영역 전체 비활성화 (opacity 0.4, pointer-events: none)

##### 영역 4: 등록정보

2열 form-grid, 전부 readonly-field.

| 필드 | 비고 |
|------|------|
| 승인상태 | Badge (승인완료) |
| 반려사유 | 텍스트 — 반려 시에만 값 표시, 정상이면 "-" |
| 등록자 | 이름 |
| 등록일시 | YYYY-MM-DD HH:mm |
| 승인자 | 이름 |
| 승인일시 | YYYY-MM-DD HH:mm |
| 수정자 | 이름 |
| 수정일시 | YYYY-MM-DD HH:mm |

##### 영역 5: 판매상태

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 판매상태 변경 | `<select>` | 판매중 / 판매일시중지 / 판매종료 |

#### 탭 2: 전시정보 (편집 가능 폼)

단일 `form-section` 카드 내 `form-grid` 2열 레이아웃. 모든 필드 필수(*).

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 전시여부 | radio: 전시(기본) / 비전시 | 전시: 전시기간 해당 시 FO 노출, 비전시: FO 비노출 |
| 노출채널 | checkbox: 전체 / PC / 모바일 | "전체" 체크 시 PC+모바일 자동 체크+disabled, 해제 시 개별 선택 |
| 전시기간 | date-range: 시작일 ~ 종료일 | 네이티브 date input. 정책: 판매기간 "기간 직접 설정" 시 전시기간이 판매기간 초과 불가 |
| 담당MD | `<input readonly>` + [조회] 버튼 | 팝업 선택 → "MDID(MD명)" 형태 출력. 샘플: MD001(김담당) |
| 표준카테고리 | 3단 `<select>` (대/중/소분류) + [등록] | 담당MD 선택 후 해당 MD의 대분류만 호출. [등록] 시 전시카테고리 테이블에 행 추가 |
| 전시카테고리 | 테이블: 대표(라디오) / 경로 / 삭제 | 대표: 라디오 단일 선택(첫 행 default). 삭제: 행 제거. 샘플 2행 |

#### 탭 3: 판매정보 (편집 가능 폼 — 5개 영역)

##### 영역 1: 판매정보 등록

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 판매기간 | checkbox "제한없음" + date-range (시작~종료) | 필수(*). 제한없음 체크 시 date disabled. 기간설정 시 전시기간 초과 불가 |
| 과세구분 | radio (과세/면세/영세) | 필수(*). default: 과세 |
| 성인인증상품 | radio (Yes/No) | 필수(*). default: No |

##### 영역 2: 가격정보

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 판매가(VAT포함) | `<input number>` | 필수(*). 숫자만 |
| 상품 마진율 | checkbox + `<input number>` (%) | 체크 시 활성. 계약 마진율 초과 불가 |
| 상품 마진 | readonly-field (자동) | 판매가 × 마진율 |
| 계약 마진율 | readonly-field (자동) | PO 계약값 |
| 계약 마진 | readonly-field (자동) | 판매가 × 계약마진율 |

##### 영역 3: 할인 프로모션 정보 (readonly 테이블)

승인완료 후 프로모션관리에서 등록. 테이블 열: 프로모션ID | 프로모션명 | 판매가 | 할인가 | 할인율

##### 영역 4: 쿠폰 프로모션 (readonly 테이블)

테이블 열: 쿠폰ID | 쿠폰명 | 정률(%) | 정액(₩)

##### 영역 5: 적립금 프로모션 (readonly 테이블)

테이블 열: 적립금ID | 적립금명 | 정률(%) | 정액(₩)

#### 탭 4: 옵션/재고 (편집 가능 폼)

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 상품옵션 | radio (사용안함/단일옵션/복수옵션) | 필수(*). default: 사용안함 |

- **사용안함 선택 시:** 재고수량 직접 입력 (input number)
- **단일/복수옵션 선택 시:** [옵션 등록] 버튼 → 팝업(optionPopup046)
  - 옵션 목록 테이블: 전시순서 | 옵션ID | 옵션1 | 옵션가격 | 재고 | 판매상태(select) | 수정
  - 재고수량: readonly (합산 자동)
  - 판매상태 select: 판매중/판매일시중지/일시품절/품절/판매종료

#### 탭 5: 배송정보 (편집 가능 폼 — 3개 영역)

##### 영역 1: 출고지 배송정보

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 출고 배송정보 | `<select>` (출고지명 목록) | 필수(*). 판매사관리 등록 데이터 |
| 택배사명 | readonly-field (자동) | select 연동 |
| 출고 배송지ID | readonly-field (자동) | |
| 출고지 주소 | readonly-field (자동) | 도로명+상세 |
| 합배송 | readonly-field | 가능/불가능 |
| 배송비 정책 | readonly-field | 무료/유료/조건무료 |
| 기본 배송비 | readonly-field | 금액 |
| 도서산간 배송비 | readonly-field | 금액 |
| 제주 배송비 | readonly-field | 금액 |
| 배송 안내 | readonly-field | 300자 |

##### 영역 2: 반품 배송정보

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 반품 회수지 | `<select>` | 필수(*). 판매사관리 등록 데이터 |
| 택배사명 | readonly-field | |
| 반품 배송지ID | readonly-field | |
| 회수지 주소 | readonly-field | |
| 반품 배송비 | readonly-field | |
| 자동회수 가능 | readonly-field | 가능/불가능 |
| 도서산간 반품 배송비 | readonly-field | |
| 제주 반품 배송비 | readonly-field | |
| 반품 기간 | readonly-field | |
| 반품 안내 | readonly-field | |

##### 영역 3: 교환정보

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 교환 회수지 | `<select>` | 필수(*) |
| 택배사명 | readonly-field | |
| 교환 배송지ID | readonly-field | |
| 회수지 주소 | readonly-field | |
| 교환 출고지 주소 | readonly-field | |
| 교환 배송비 | readonly-field | |
| 자동회수 가능 | readonly-field | 가능/불가능 |
| 도서산간 교환 배송비 | readonly-field | |
| 제주 교환 배송비 | readonly-field | |
| 교환 기간 | readonly-field | |
| 교환 안내 | readonly-field | |

#### 탭 6: 상세정보 (편집 가능 폼 — 3개 영역)

##### 영역 1: 대표 이미지 등록

- 이미지 최대 5개, 최초 등록 시 대표 자동 설정
- 테이블: 대표(라디오) | 파일명 | 등록 | 삭제
- 미리보기 placeholder (120×120) + alt text

##### 영역 2: 동영상 등록

- mp4 최대 1개
- 파일명 | 등록 | 삭제
- FO 노출 순서: 동영상 > 이미지

##### 영역 3: 상품상세설명

- 적용 범위 radio (전체/PC)
  - 전체: PC 편집기 내용이 모바일에 자동 적용 → 모바일 편집기 비활성
  - PC: 모바일 편집기 영역 활성화 → PC/모바일 각각 등록
- PC HTML 편집기: editor-placeholder
- 모바일 HTML 편집기: editor-placeholder (전체 선택 시 disabled)

#### 탭 7: 상품정보제공고시 (편집 가능 폼)

| 필드 | UI 유형 | 비고 |
|------|---------|------|
| 입력방식 | radio (연동/직접입력) | 필수(*) |
| 상품분류 | `<select>` | 필수(*). 표준카테고리 대분류 기준 중카테고리 |
| 상품정보제공고시 | 편집 테이블 | 번호(input) / 항목(input) / 내용(input) / +/- |

- 연동: 상품분류 선택 시 항목 자동 출력 → 수정/추가/삭제 가능
- 직접입력: 빈 상태에서 항목 직접 추가
- 상품분류 변경 시 항목 초기화 얼럿 정책
- 10행 이상 시 세로 스크롤 (max-height: 280px)

### 승인 이력

- **기본정보 탭에서만 표시** (영역 1.6)
- 테이블: No, 요청일, 처리일, 처리자, 처리상태(Badge), 비고

### 하단 버튼

| 버튼 | 동작 | 비고 |
|------|------|------|
| 취소 | BO-PDM-040 목록 화면 이동 | 항상 표시 |
| 미리보기 | placeholder 모달 표시 | 항상 표시 |
| 저장 | 저장 확인 모달 표시 | primary 버튼 |

### 팝업 (5개)

| 팝업 | ID | 구성 |
|------|----|------|
| 판매사 조회 | vendorPopup046 | 검색 + 라디오 목록(코드/판매사명) |
| 브랜드 조회 | brandPopup046 | 검색 + 라디오 목록(코드/브랜드명) |
| 담당MD 조회 | mdPopup046 | 검색 + 라디오 목록(MD코드/MD명). 샘플 3행: MD001 김담당, MD002 이담당, MD003 박담당 |
| 고객사 조회 | clientPopup046 | 검색 + 라디오 목록(코드/고객사명) |
| 옵션 등록 | optionPopup046 | 상품코드(readonly), 상품명(readonly), 옵션여부. 옵션 속성 테이블: 번호/옵션ID(자동)/옵션명(input, 15자)/등록·삭제. [닫기]/[저장] |

## 기능 명세

- 기본정보 탭: 편집 가능 폼 (5개 영역)
- 전시정보 탭: 편집 가능 폼 (6개 필드)
- 판매정보 탭: 편집 가능 폼 (5개 영역 — 판매정보등록, 가격정보, 할인/쿠폰/적립금 프로모션)
- 옵션/재고 탭: 편집 가능 폼 (옵션 라디오 + 옵션등록 팝업 + 옵션목록 테이블)
- 배송정보 탭: 편집 가능 폼 (3개 영역 — 출고지/반품/교환)
- 상세정보 탭: 편집 가능 폼 (3개 영역 — 이미지/동영상/상세설명)
- 상품정보제공고시 탭: 편집 가능 폼 (입력방식/상품분류/편집테이블)
- 탭 전환: JavaScript `switchTab(tabId)` 함수
- 키워드 등록: `addKeyword()` — 입력값을 태그(chip)로 추가
- 태그 삭제: `removeTag(el)` — 키워드/고객사 태그 공용 삭제
- 구매수량 토글: `toggleQtyField(name)` — 사용안함/사용 라디오에 따라 숫자 입력 활성/비활성
- 고객사 섹션 토글: `toggleClientSection()` — 미등록/등록 전환 시 입력 영역 활성/비활성
- 노출채널 전체 토글: `toggleChannelAll()` — "전체" 체크 시 PC/모바일 자동 체크+disabled
- 전시카테고리 등록: `addDisplayCategory()` — 표준카테고리 3단 select 값을 전시카테고리 테이블에 행 추가
- 전시카테고리 삭제: `removeDisplayCategory(el)` — 전시카테고리 테이블 행 제거
- 담당MD 선택: `selectMd046()` — 팝업에서 선택한 MD를 "MDID(MD명)" 형태로 입력 필드에 출력
- 팝업 열기/닫기: `openPopup046(id)` / `closePopup046(id)`
- 고객사 등록: `addClient()` — 팝업 선택값을 태그로 추가
- 모달 열기/닫기: `openModal(id)` / `closeModal(id)`
- 판매기간 토글: `toggleSalePeriod()` — 제한없음 체크 시 date input disabled
- 상품마진율 토글: `toggleMarginRate()` — 체크박스로 마진율 입력 활성/비활성
- 옵션 사용 토글: `toggleOptionUse()` — 사용안함/단일/복수 라디오에 따라 영역 전환
- 상세정보 범위 토글: `toggleDetailScope()` — 전체/PC 라디오에 따라 모바일 편집기 활성/비활성
- 고시 입력방식 토글: `toggleNoticeMode()` — 연동/직접입력 전환
- 고시 행 추가: `addNoticeRow()` — 상품정보제공고시 항목 행 추가
- 고시 행 삭제: `removeNoticeRow(el)` — 상품정보제공고시 항목 행 삭제
